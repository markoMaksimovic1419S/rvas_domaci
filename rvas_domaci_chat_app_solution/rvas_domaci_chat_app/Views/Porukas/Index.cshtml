@model IEnumerable<rvas_domaci_chat_app.Models.Poruka>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.poruku_poslao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.text_poruke)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_sobe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.poruku_poslao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.text_poruke)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_sobe)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
